import React from "react";
import axios from "axios";

const App: React.FC = () => {
  return (
    <div>
      <h1>
        Hello, Vite + React + TypeScript!{import.meta.env.VITE_REACT_VITE}sad
      </h1>
    </div>
  );
};

export default App;
